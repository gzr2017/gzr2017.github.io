$(document).ready(function () {
    $('.materialboxed').materialbox();
    $('.tabs').tabs();
    $('.owl-carousel').owlCarousel();
});
$('.main-carousel').flickity({
    // options
    cellAlign: 'left',
    contain: true,
    autoPlay: true,
    draggable: true,
    freeScroll: true
});